name: Database Backup

# ⛔ انتقل النسخ الاحتياطي إلى المستودع الخاص dsd-ops-vault (27 أغسطس 2026).
#
# السبب: artifacts المستودع **العام** يحمّلها أي مستخدم GitHub — أي أن نسخ
# قاعدة العملاء كاملةً كانت متاحة للعموم. حُذفت النسخ المكشوفة كلها، وصار
# النسخ اليومي (للقاعدتين: المنصّة والبوت) + نسخة باردة شهرية هناك.
#
# يبقى هذا الملف شاهدَ توجيهٍ كي لا يعيد أحدٌ بناء النسخ هنا.

on:
  workflow_dispatch: {}

jobs:
  moved:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::النسخ انتقل إلى aliorini1996-sudo/dsd-ops-vault — لا نسخ في مستودع عام"
          exit 1
