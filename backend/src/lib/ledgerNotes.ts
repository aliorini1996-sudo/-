// ============================================================================
// وسوم صفوف الدفتر المساعد (account_entries) التي يعتمد عليها طرفان: كاتبُ الصفّ والمُطابِق الذي يشتقّ منه الحدث.
// ----------------------------------------------------------------------------
// وحدةٌ نقيّة بلا اعتماديات عمداً: يستوردها services/invoiceVoid.ts (يكتب) وservices/gl/sync/desired.ts (يقرأ)،
// ولا يستورد أحدهما الآخر (حارس tests/gl-hooks-static.test.ts: مسارات التشغيل لا تعرف الدفاتر).
// ============================================================================

/**
 * ZATCA Z5.4 / قرار المالك Q1 — وصف صفّ INVOICE_CREDIT للإبطال **الجزئيّ** لفاتورة نقدية قياسية رفضتها الهيئة:
 * شقّ الفاتورة وحده يُعكس والمحصَّل يبقى رصيداً دائناً للعميل. هذا الوسم هو دليلُ الجزئية الثابت: يُكتب مرّةً مع
 * الصفّ ولا يتغيّر بعدها — بخلاف مرآة الفاتورة (`einvoiceStatus`) التي تُكتب لاحقاً فتقلب اشتقاقاً سابقاً.
 * لا يُغيَّر نصّه إلا مع تغيير كاتبه وقارئه معاً (tests/gl-sync-desired-events.test.ts يحرسه).
 */
export const CASH_VOID_KEEP_COLLECTION_NOTE = 'إبطال فاتورة نقدية رفضتها الهيئة — المحصَّل رصيد دائن للعميل';
